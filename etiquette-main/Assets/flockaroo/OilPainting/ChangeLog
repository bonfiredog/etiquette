v 1.7
* URP reinit fix
* new linear2Gamma parameter

v 1.6
* URP fix: wasnt able to render to color fmt (at least in play node) 
  ...render 1st float buffer then copy to final
  
v 1.5
* HDRP crash bug fix (Regex)

v 1.4:
* added URP support (disabled by #USE_URP define due to backwards compatibility)

v 1.3:
* init EffectFade to 1.0 in HDRP

v 1.2:
* HDRP support (disabled by #USE_HDRP define due to backwards compatibility)
* HDRP gamma correct (linear colorspace)
* some y-flip fixes

v 1.1:
* input feature: texture as input instead of cam
* output feature: render to texture instead of screen
* color slider (adjustible color intensity)
* color spread (adjustible color randomness)
* frame latency fix (buffer order)

